# TrackMaker の依存ライブラリ
# コア機能（作曲エンジン、MIDI/MOD/S3M/XM/IT/DMF エクスポート）は
# Python標準ライブラリのみで動作するため、必須の依存関係はありません。
#
# --config に .yaml/.yml ファイルを指定する場合のみ PyYAML が必要です
# （.json 設定ファイルなら標準ライブラリのみで動作します）。
pyyaml>=6.0
